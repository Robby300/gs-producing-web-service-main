pipeline {
    agent any

    stages {
        stage('deploy to server') {
            steps {
                sshagent(['server']) {
                     sh 'ssh robert@192.168.233.128 bash deploy.sh'
                }
            }
        }
    }
}