pipeline {
    agent any

    stages {
        stage('deploy to server') {
            steps {
                sshagent(['server']) {
                   sh 'ssh robert@192.168.233.131 bash deploy-compose'
                }
            }
        }
    }
}